/**
 * Centralized configuration for the browser extension.
 */
export const config = {
  // Default server URL. In the future, this could be loaded from extension storage
  // so the user can easily point to a different Sigil core instance.
  SERVER_URL: 'http://localhost:7445',
};
