// Core TSConfig: Extends root config. Compiles src/ and bin/ to the dist/ directory for the Node.js backend.
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "."
  },
  "include": ["src/**/*", "bin/**/*"],
  "exclude": ["node_modules", "dist"]
}
